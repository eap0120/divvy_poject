{
  "name": "node-api",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.0.2",
    "express": "^4.0.0",
    "morgan": "^1.8.1",
    "mysql": "~2.13.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  },

  "scripts" : {
    "start:dev": "nodemon ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
   }
}
